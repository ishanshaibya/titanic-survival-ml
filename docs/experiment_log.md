# Experiment Log

Tracks every model/feature combination actually tried, with real results —
nothing here is estimated or assumed.

**Defaults used throughout, unless a row says otherwise:** stratified
train/validation split (`stratify=y`), 80/20 split. "Accuracy" below is the
**mean across 5 different random_states (1, 7, 42, 55, 99)**, not a single
run, since we found single-split accuracy varies by 2-3 points just from
random chance. Range and standard deviation are included so a difference
between rows can be judged against normal run-to-run noise, not just eyeballed.

Baseline to beat (predicting "everyone dies" for every passenger): **61.62%**.

| # | Model | Features | Accuracy (mean / range / stdev) | Notes | Next step |
|---|---|---|---|---|---|
| 1 | Logistic Regression | IsFemale, Pclass, FamilySize, Title_* (Mr/Miss/Mrs/Master/Rare, one-hot) — 8 total | 83.46% / 81.56–84.92% / ±1.35 pts | First baseline. Learned weights matched EDA directionally (IsFemale +1.57, Pclass −0.99, Title_Mr −1.39). Beats the 61.62% baseline by ~22 points. | Add more features (Age, Fare, Cabin, Embarked) and see if accuracy improves. |
| 2 | Logistic Regression | Adds Age (median-by-Title imputed), Fare, HasCabin, Embarked_* (one-hot) — 13 total | 83.35% / 81.01–86.03% / ±1.91 pts | **No real improvement** over row 1 — mean is actually slightly lower, spread is wider. Tested feature scaling separately (StandardScaler): fixed a solver convergence warning, did not change accuracy. | Logistic regression may not be able to represent FamilySize's non-linear (rise-then-collapse) survival pattern found in EDA. Try a model type that can — decision tree next. |
| 3 | Decision Tree | Same 13 features as row 2 | Swept max_depth ∈ {2,3,4,5,6,8}. Best: **depth=3, 81.90%** (range 78.21–84.36%). Depth 2 underfits (79.22%); depth 5+ overfits, declining to ~80.0–80.5%. Classic underfit→peak→overfit curve, peak at depth 3-4. | **Hypothesis rejected**: even at its best-tuned depth, the decision tree (81.90%) underperforms both logistic regression versions (83.46%, 83.35%). Checked feature_importances_ at depth=3: Title_Mr dominates (0.637), IsFemale unused (0.000) — the tree treats Title_Mr as a full substitute for sex and never needs IsFemale once it has that split. FamilySize got some use (0.083, ranked 3rd) but not enough to lift overall accuracy. | Try Random Forest — many trees, each seeing random feature subsets, might stop one feature (Title_Mr) from crowding out others (IsFemale) the way it did in a single tree. |
| 4 | Random Forest | Same 13 features as row 2/3 | Swept max_depth ∈ {3,5,7,None} × n_estimators ∈ {50,100,200} × 5 random_states (60 forests total). Best mean: **depth=5, n_estimators=200 → 84.25%**, but stdev ±2.45 — the widest spread of any model tried. depth=None (unlimited): much more stable (stdev as low as 0.73) but mean drops to ~80%. | Feature importance at depth=3 confirmed the hypothesis partially: IsFemale went from 0.000 (single tree) to 0.220 (forest) — many trees without Title_Mr available were forced to use IsFemale instead. FamilySize importance actually *decreased* slightly (0.083→0.047), not the outcome originally hoped for. **Overall: Random Forest's best result is not reliably better than logistic regression** — the apparent edge (84.25% vs 83.46%) is smaller than the forest's own run-to-run noise, so it can't be called a real win. | Logistic regression remains the most defensible choice so far, given comparable accuracy with lower variance and full interpretability. Try investigating the Ticket feature (group size via shared tickets) before further model tuning — the feature set itself, not just the model, may be the real bottleneck. |
| 5 | Logistic Regression | Adds TicketGroupSize (14 total) — built by combining Ticket values from train.csv AND test.csv via pd.concat before counting, to correctly capture families split across both files (e.g. the Sage family: 7 rows in train, 4 in test, true group size 11) | 83.69% / 82.12–86.59% / ±1.74 pts | Small improvement over row 2 (83.35% / ±1.91) — mean up 0.34 pts AND stdev down, a more consistent signal than most of today's other attempts. Confirmed via real data (Chip/Lam/Ling group, ticket 1601) that TicketGroupSize captures genuine non-family travel groups FamilySize misses; also confirmed (Sage family) that computing it from train.csv alone undercounts split families — fixed by combining both files' Ticket column (safe: uses no Survived/target information, so not data leakage). | Promising but not confidently proven — 5-split ranges still overlap substantially with row 2's. Proper cross-validation (more folds, Stage 9) needed before calling this a real win. |
| 6 | Random Forest | Same 14 features (incl. TicketGroupSize) | **84.40%** / std 0.92 (n_estimators=100, max_depth=8) | Switched measurement method to sklearn's cross_val_score with StratifiedKFold(shuffle=True) instead of manual random_state loops — more systematic (every row validated exactly once per sweep) and, once shuffling was explicitly enabled, gave tighter/more trustworthy estimates across all models. Swept depth 2-20 at n_estimators=100: accuracy rises to a clear peak at depth=8, then declines (82.15% by depth=20) — confirms the same underfit→peak→overfit shape as the single tree, just occurring later since averaging 100 trees resists overfitting longer. This is the best result of any model tried so far, and unlike yesterday's Random Forest attempt, its std (0.92) is now *tighter* than Logistic Regression's (1.44), not wider — a confident, not just lucky, win. | Confirmed best model to date: Random Forest (n_estimators=100, max_depth=8). Try KNN and SVM next for completeness before finalizing model choice. |